# Google OAuth Configuration
# Replace the placeholders below with your OAuth client values from
# Google Cloud Console. Do NOT commit actual secrets to source control.
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Flask secret for session signing. Use a secure random value in production.
SECRET_KEY=replace-with-a-secure-random-string

GOOGLE_REDIRECT_URI=use-redirected-link-used-in-oauth

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Important: Make sure the OAuth redirect URI in Google Cloud Console
# includes: http://localhost:5000/callback (or your app's public callback URL)